version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci 
    build:
      commands:
        - npm run build
     postbuild:
      commands:
        - ng serve -o
  artifacts:
    baseDirectory: dist/categoria
    files:
      - '*/'
  cache:
    paths:
      - .npm/*/